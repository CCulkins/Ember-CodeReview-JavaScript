import { postAwards } from 'message-board/helpers/post-awards';
import { module, test } from 'qunit';

module('Unit | Helper | post awards');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postAwards([42]);
  assert.ok(result);
});
